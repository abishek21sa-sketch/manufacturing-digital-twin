# Demo artifacts

`RUN_DEMO.cmd` generates `latest_demo_evidence.json` here from the dedicated deterministic demo database. Generated demo evidence is intentionally not pre-shipped as realized plant evidence.
